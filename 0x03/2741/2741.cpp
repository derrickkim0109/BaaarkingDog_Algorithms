#include <bits/stdc++.h>
using namespace std;

int main(void) {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int inputValue;
    cin >> inputValue;

    for(int i=1; i <=inputValue; i++) {
        cout << i << "\n";
    }
}